<form method="post" action="{{ path('app_constelation_delete', {'id': constelation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ constelation.id) }}">
    <button class="btn">Delete</button>
</form>
