<form method="post" action="{{ path('app_etoile_delete', {'id': etoile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etoile.id) }}">
    <button class="btn">Delete</button>
</form>
